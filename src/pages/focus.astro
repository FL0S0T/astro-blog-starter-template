---
import BaseHead from "../components/BaseHead.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import I18nScript from "../components/I18nScript.astro";
import CookieConsent from "../components/CookieConsent.astro";
import { CATEGORIES } from "../consts";

const layerDescriptions = [
	"How technology changes systems. Not individual tools, but the architecture of decisions, organizations and markets.",
	"From idea to system. Operational insights into building technology-driven companies.",
	"Technologies in practice. Real experiences with LLMs, agents, infrastructure and new paradigms.",
	"What new technologies mean for large organizations. Budgeting, risk and positioning.",
	"How technology changes capital allocation, valuation logic and financing structures.",
];
---

<html lang="en">
	<head>
		<BaseHead
			title="Focus"
			description="Fuenf Denkebenen fuer strukturelles Denken im KI-Zeitalter."
		/>
	</head>
	<body>
		<Header />

		<section class="page-hero" id="hero">
			<div class="hero-inner">
				<h1 data-i18n="focus.title">Five Thinking Layers</h1>
				<p data-i18n="focus.sub">
					Technology doesn't just change tools — it changes structures, decisions and capital flows. These five layers form the framework of my analyses.
				</p>
			</div>
		</section>

		<main>
			<section class="layers">
				{CATEGORIES.map((cat, i) => (
					<a href={`/insights/?category=${cat.slug}`} class="layer-item">
						<div class="layer-header">
							<span class="layer-number">{String(i + 1).padStart(2, "0")}</span>
							<h2>{cat.label}</h2>
						</div>
						<p data-i18n={`layer.${i + 1}`}>{layerDescriptions[i]}</p>
						<span class="layer-link" data-i18n="focus.readArticles">
							Read articles
							<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
								<line x1="5" y1="12" x2="19" y2="12"></line>
								<polyline points="12 5 19 12 12 19"></polyline>
							</svg>
						</span>
					</a>
				))}
			</section>

			<section class="why-section">
				<div class="why-inner">
					<h2 data-i18n="focus.why.title">Why this structure?</h2>
					<p data-i18n="focus.why.p1">
						Most blogs sort by date. That creates a feed, not orientation.
					</p>
					<p data-i18n="focus.why.p2">
						These five thinking layers form an architecture. They help to contextualize developments — within larger contexts that are relevant for strategic decisions.
					</p>
				</div>
			</section>
		</main>
		<Footer />
		<CookieConsent />
		<I18nScript />
	</body>
</html>

<style>
	.page-hero {
		--mx: 0.5;
		--my: 0.5;
		background: var(--gradient-hero);
		padding: 5em 1.5em 4em;
		text-align: center;
		position: relative;
		overflow: hidden;
		isolation: isolate;
	}

	.page-hero::before,
	.page-hero::after {
		content: "";
		position: absolute;
		inset: 0;
		pointer-events: none;
		z-index: 0;
		will-change: background;
	}

	.page-hero::before {
		background: radial-gradient(
			600px circle at calc(var(--mx) * 100%) calc(var(--my) * 100%),
			rgba(79, 124, 255, 0.18) 0%,
			transparent 70%
		);
	}

	.page-hero::after {
		background: radial-gradient(
			500px circle at calc(var(--mx) * 100% + 80px) calc(var(--my) * 100% - 60px),
			rgba(99, 66, 255, 0.12) 0%,
			transparent 70%
		);
	}

	:global([data-theme="light"]) .page-hero::before {
		background: radial-gradient(
			600px circle at calc(var(--mx) * 100%) calc(var(--my) * 100%),
			rgba(0, 113, 227, 0.22) 0%,
			transparent 70%
		);
	}

	:global([data-theme="light"]) .page-hero::after {
		background: radial-gradient(
			500px circle at calc(var(--mx) * 100% + 80px) calc(var(--my) * 100% - 60px),
			rgba(120, 100, 220, 0.15) 0%,
			transparent 70%
		);
	}

	@media (hover: none) {
		.page-hero::before {
			background: radial-gradient(600px circle at 50% 50%, rgba(79, 124, 255, 0.15) 0%, transparent 70%);
			animation: nebula-drift-1 8s ease-in-out infinite alternate;
		}
		.page-hero::after {
			background: radial-gradient(500px circle at 50% 50%, rgba(99, 66, 255, 0.10) 0%, transparent 70%);
			animation: nebula-drift-2 10s ease-in-out infinite alternate;
		}
		:global([data-theme="light"]) .page-hero::before {
			background: radial-gradient(600px circle at 50% 50%, rgba(0, 113, 227, 0.18) 0%, transparent 70%);
		}
		:global([data-theme="light"]) .page-hero::after {
			background: radial-gradient(500px circle at 50% 50%, rgba(120, 100, 220, 0.12) 0%, transparent 70%);
		}
	}

	@keyframes nebula-drift-1 {
		0%   { transform: translate(-8%, -5%); }
		100% { transform: translate(8%, 5%); }
	}

	@keyframes nebula-drift-2 {
		0%   { transform: translate(6%, 8%); }
		100% { transform: translate(-6%, -8%); }
	}

	@media (prefers-reduced-motion: reduce) {
		.page-hero::before,
		.page-hero::after {
			animation: none !important;
			transform: none !important;
		}
	}

	.hero-inner {
		max-width: 600px;
		margin: 0 auto;
		position: relative;
		z-index: 1;
	}

	.hero-inner h1 {
		margin-bottom: 0.75em;
	}

	.hero-inner p {
		color: var(--text-secondary);
		font-size: 1.15em;
		line-height: 1.7;
	}

	main {
		width: var(--max-width-wide);
		max-width: calc(100% - 2rem);
		margin: 0 auto;
		padding: 3rem 1rem;
	}

	.layers {
		display: grid;
		gap: 1em;
		margin-bottom: 4em;
	}

	.layer-item {
		display: block;
		padding: 2em 2.5em;
		background: var(--surface);
		border: 1px solid var(--border);
		border-radius: var(--radius-lg);
		text-decoration: none;
		color: inherit;
		transition: all 0.25s ease;
		position: relative;
		overflow: hidden;
	}

	.layer-item::after {
		content: "";
		position: absolute;
		top: -50%;
		left: 0;
		width: 35%;
		height: 200%;
		background: linear-gradient(
			90deg,
			transparent 0%,
			rgba(255, 255, 255, 0.03) 35%,
			rgba(255, 255, 255, 0.07) 50%,
			rgba(255, 255, 255, 0.03) 65%,
			transparent 100%
		);
		transform: translateX(-100%) rotate(35deg);
		pointer-events: none;
		z-index: 1;
	}

	.layer-item:hover::after {
		animation: glass-sweep 0.6s ease-out forwards;
	}

	:global([data-theme="light"]) .layer-item::after {
		background: linear-gradient(
			90deg,
			transparent 0%,
			rgba(255, 255, 255, 0.4) 35%,
			rgba(255, 255, 255, 0.7) 50%,
			rgba(255, 255, 255, 0.4) 65%,
			transparent 100%
		);
	}

	@media (prefers-reduced-motion: reduce) {
		.layer-item:hover::after { animation: none; }
	}

	.layer-item:hover {
		text-decoration: none;
		color: inherit;
		border-color: var(--border-strong);
		transform: translateY(-2px);
		box-shadow: var(--shadow-md);
	}

	.layer-header {
		display: flex;
		align-items: center;
		gap: 1em;
		margin-bottom: 0.75em;
	}

	.layer-number {
		font-size: 0.8em;
		color: var(--accent);
		font-weight: 700;
		font-variant-numeric: tabular-nums;
	}

	.layer-item h2 {
		font-size: 1.25em;
		margin: 0;
	}

	.layer-item p {
		color: var(--text-secondary);
		margin-bottom: 1em;
		line-height: 1.65;
		font-size: 0.95em;
	}

	.layer-link {
		display: inline-flex;
		align-items: center;
		gap: 0.5em;
		color: var(--accent);
		font-size: 0.9em;
		font-weight: 500;
	}

	.layer-item:hover .layer-link svg {
		transform: translateX(4px);
	}

	.layer-link svg {
		transition: transform 0.25s ease;
	}

	.why-section {
		background: var(--surface);
		border: 1px solid var(--border);
		border-radius: var(--radius-xl);
		padding: 3em;
	}

	.why-inner {
		max-width: 560px;
	}

	.why-inner h2 {
		margin-bottom: 1em;
	}

	.why-inner p {
		color: var(--text-secondary);
		line-height: 1.8;
	}

	@media (max-width: 768px) {
		.page-hero {
			padding: 3.5em 1.25em 3em;
		}

		.layer-item {
			padding: 1.5em;
		}

		.why-section {
			padding: 2em;
		}
	}

	@media (max-width: 480px) {
		.page-hero {
			padding: 2.5em 1em 2em;
			text-align: left;
		}

		.hero-inner p {
			font-size: 1em;
		}

		.layer-item {
			padding: 1.25em;
		}

		.why-section {
			padding: 1.5em;
		}
	}
</style>

<script>
	function initHeroNebula() {
		const hero = document.getElementById("hero");
		if (!hero) return;

		const prefersReducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)").matches;
		const hasHover = window.matchMedia("(hover: hover)").matches;

		if (prefersReducedMotion || !hasHover) return;

		let targetX = 0.5;
		let targetY = 0.5;
		let currentX = 0.5;
		let currentY = 0.5;
		let rafId: number | null = null;

		function onMouseMove(e: MouseEvent) {
			const rect = hero!.getBoundingClientRect();
			targetX = (e.clientX - rect.left) / rect.width;
			targetY = (e.clientY - rect.top) / rect.height;
		}

		function animate() {
			currentX += (targetX - currentX) * 0.04;
			currentY += (targetY - currentY) * 0.04;
			hero!.style.setProperty("--mx", currentX.toFixed(4));
			hero!.style.setProperty("--my", currentY.toFixed(4));
			rafId = requestAnimationFrame(animate);
		}

		const observer = new IntersectionObserver(
			([entry]) => {
				if (entry.isIntersecting) {
					hero!.addEventListener("mousemove", onMouseMove);
					if (!rafId) rafId = requestAnimationFrame(animate);
				} else {
					hero!.removeEventListener("mousemove", onMouseMove);
					if (rafId) { cancelAnimationFrame(rafId); rafId = null; }
				}
			},
			{ threshold: 0 }
		);

		observer.observe(hero);
	}

	initHeroNebula();
	document.addEventListener("astro:after-swap", initHeroNebula);
</script>
